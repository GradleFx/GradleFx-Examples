type = 'swc'

//the default for a 'swc' library project is 'external'
frameworkLinkage = 'merged'

srcDirs = [
	'src/main/as'
]

resourceDirs = [
	'src/main/asresources'
]

dependencies {
	external project(':util')
}
